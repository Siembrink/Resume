{% block nav %}
    <nav class="p-5 dark:bg-gray-700">
        <div class="max-w-screen-xl px-4 py-3 mx-auto">
            <div class="flex items-center">
                <ul class="flex flex-row t-0 space-x-8 rtl:space-x-reverse text-xl">
                    <li>
                        <a href="{{ path('home') }}" class="text-gray-900 dark:text-white hover:underline" aria-current="page">Home</a>
                    </li>
                    <li>
                        <a href="{{ path('experience') }}" class="text-gray-900 dark:text-white hover:underline">Ervaring</a>
                    </li>
                    <li>
                        <a href="{{ path('projects') }}" class="text-gray-900 dark:text-white hover:underline">Projecten</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}